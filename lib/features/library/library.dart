/// Public API for the `library` feature.
///
/// Cross-feature imports must go through this barrel — never reach into a
/// feature's internals (see docs/standards/architecture-standards.md).
library;

export 'presentation/screens/library_screen.dart';
