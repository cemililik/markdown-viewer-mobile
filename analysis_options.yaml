# Analyzer + linter configuration.
#
# Binding rules live in docs/standards/coding-standards.md and
# docs/standards/architecture-standards.md. This file is the executable
# enforcement of those rules. Any change here that loosens enforcement
# requires an ADR.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # Riverpod / freezed / drift generators emit annotations that the
    # analyzer flags otherwise.
    invalid_annotation_target: ignore
    # Treat unawaited futures as errors, not warnings.
    unawaited_futures: error
    # Treat missing required parameters as errors.
    missing_required_param: error
    missing_return: error

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.config.dart"
    - "**/*.gr.dart"
    - "**/*.mocks.dart"
    - "lib/l10n/generated/**"
    - "build/**"
    - ".dart_tool/**"

linter:
  rules:
    # Style — enforced
    always_declare_return_types: true
    always_use_package_imports: true
    avoid_dynamic_calls: true
    avoid_print: true
    avoid_unused_constructor_parameters: true
    cancel_subscriptions: true
    close_sinks: true
    directives_ordering: true
    no_adjacent_strings_in_list: true
    no_runtimeType_toString: true
    prefer_asserts_with_message: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_relative_imports: false
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_constructors_first: true
    sort_pub_dependencies: false
    sort_unnamed_constructors_first: true
    test_types_in_equals: true
    throw_in_finally: true
    unawaited_futures: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    unnecessary_null_aware_assignments: true
    unnecessary_parenthesis: true
    unnecessary_raw_strings: true
    unnecessary_statements: true
    unnecessary_string_interpolations: true
    use_build_context_synchronously: true
    use_super_parameters: true

    # Disabled — superseded by stricter rules above or by team preference.
    avoid_classes_with_only_static_members: false
